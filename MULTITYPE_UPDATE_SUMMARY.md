# 多类型消息支持更新说明

## 更新概览
为 **"群发"** 和 **"修改"** 两个功能添加了多类型消息支持，不仅限于发送纯文本内容。

---

## 修改详情

### 1. `send_msg_all_groups()` 函数 (第 355-385 行)

**改进内容：**
- ✅ 支持字符串类型消息（纯文本）
- ✅ 支持 `Manager.Message` 对象（含多种消息段）
- ✅ 支持消息段列表 - 自动过滤 `Reply` 段
- ✅ 完善的错误处理

**功能说明：**
```python
# 处理逻辑
- 若消息为字符串 → 转换为 Text 段
- 若消息为 Manager.Message → 直接使用
- 若消息为列表 → 过滤 Reply 段后重建消息
- 其他情况 → 转换为字符串后作为 Text 段
```

---

### 2. `timing_message()` 函数 (第 314-351 行)

**改进内容：**
- ✅ 支持 JSON 格式的多类型消息序列化
- ✅ 自动从 `timing_message.ini` 中解析和重建消息
- ✅ 降级处理：JSON 解析失败时作为纯文本处理

**支持的消息类型：**
- Text（文本）
- Image（图片）
- Voice（语音）
- At（@提及）

**配置格式示例：**
```
HH:MM⊕[{"type":"text","data":"内容"},{"type":"image","data":"url"}]
```

---

### 3. "修改" 命令功能 (第 1251-1299 行)

**改进内容：**
- ✅ 支持引用消息转发（新增转发模式）
- ✅ 自动检测引用消息中的多种内容类型
- ✅ 消息预览显示（纯文本或"多类型消息"标签）
- ✅ 完整错误处理

**使用方式：**

**纯文本模式：**
```
/修改 HH:MM 定时消息内容
```

**转发模式（新增）：**
```
1. 引用一条消息
2. 发送：/修改 HH:MM
3. 系统将定时转发该引用消息（包含文本、图片、语音等）
```

---

### 4. "群发" 命令功能 (第 1309-1345 行)

**改进内容：**
- ✅ 支持引用消息转发（新增转发模式）
- ✅ 自动检测引用消息内容类型
- ✅ 纯文本模式兼容保留
- ✅ 完善的日志和错误提示

**使用方式：**

**纯文本模式：**
```
/群发 要群发的文本消息内容
```

**转发模式（新增）：**
```
1. 引用一条消息（包含图片、语音等）
2. 发送：/群发
3. 系统将转发该消息到所有群组（除黑名单外）
```

**帮助信息更新：**
- 已更新提示文本，包含新的转发模式说明

---

## 支持的消息类型

| 消息类型 | 支持范围 | 说明 |
|---------|---------|------|
| Text | 群发、定时消息 | 纯文本内容 |
| Image | 群发、定时消息 | 图片 URL |
| Voice | 群发、定时消息 | 语音 URL |
| At | 群发、定时消息 | @用户提及 |
| Reply | 仅用于转发 | 引用消息（自动过滤不转发） |

---

## 技术细节

### 消息序列化格式

在 `timing_message.ini` 中使用 JSON 格式存储多类型消息：

```json
[
  {"type": "text", "data": "消息文本"},
  {"type": "image", "data": "https://example.com/image.jpg"},
  {"type": "voice", "data": "https://example.com/voice.mp3"},
  {"type": "at", "data": 123456789}
]
```

### 兼容性

- ✅ 与现有的纯文本配置完全兼容
- ✅ JSON 解析失败时自动降级为纯文本处理
- ✅ 保留所有原有功能和权限检查

---

## 使用示例

### 示例 1：转发一条包含图片的消息到所有群

```
1. 在群中引用一条包含图片的消息
2. 输入：/群发
3. 结果：该消息（带图片）被转发到所有群组
```

### 示例 2：设置定时转发多媒体消息

```
1. 引用一条包含图片+文本的消息
2. 输入：/修改 10:00
3. 每天 10:00 系统自动转发该消息到所有群组
```

---

## 修改清单

- [x] 更新 `send_msg_all_groups()` 函数
- [x] 更新 `timing_message()` 函数  
- [x] 更新 "修改" 命令处理逻辑
- [x] 更新 "群发" 命令处理逻辑
- [x] 更新帮助文本提示
- [x] 添加错误处理机制
- [x] 测试兼容性

---

## 注意事项

1. **消息大小限制**：请注意 QQ 的消息大小限制，过大的消息可能无法发送
2. **速率限制**：群发消息会在每条消息间随机延迟 0-3 秒
3. **黑名单**：群发时仍会遵守黑名单配置
4. **权限**：所有操作需要 ADMINS 权限
5. **存储**：多类型消息配置存储在 `timing_message.ini` 中

---

## 更新时间

- 更新日期：2026-01-09
- 更新版本：3.0+
- 兼容性：完全向后兼容
